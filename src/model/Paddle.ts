export class Paddle {
  public paddleWidth: number;
  public paddleHeight: number;


  constructor(paddleWidth: number, paddleHeight: number) {
    this.paddleWidth = paddleWidth;
    this.paddleHeight = paddleHeight;
  }
}